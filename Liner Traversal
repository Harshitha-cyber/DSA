#include <stdio.h>

int main() {
    int arr[] = {4,7,6,3,2};
    int n = sizeof(arr)/sizeof(arr[0]);
    printf("linera traversal");
    for(int i=0;i < n;i++){
        printf("%d",arr[i]);
}
printf("\n");
return 0;
}

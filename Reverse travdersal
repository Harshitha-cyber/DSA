#include <stdio.h>

int main() {
    int arr[] = {4,7,6,3,2};
    int n = sizeof(arr)/sizeof(arr[0]);
    printf(" Reverse linera traversal");
    for(int i=n-1; i>=0;i--){
        printf("%d",arr[i]);
}
printf("\n");
return 0;
}

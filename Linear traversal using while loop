#include <stdio.h>

int main() {
    int arr[] = {4,7,6,3,2};
    int n = sizeof(arr)/sizeof(arr[0]);
    int i = 0;
    printf("linera traversal using while loop");
    while(i<n){
        printf("%d",arr[i]);
        i++;
}
printf("\n");
return 0;
}
